@model IEnumerable<VJP_Entity.JobApplyActivity>

@{
    ViewBag.Title = "AppliedJobs";
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

<h2>AppliedJobs</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobPost.JobTitle)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.ApplyDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JobPost.JobTitle)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.ApplyDate)
        </td>
        <td>
            @Html.ActionLink("ViewJob", "ViewJob", new { id=item.Id }) |
             </td>
    </tr>
}

</table>
